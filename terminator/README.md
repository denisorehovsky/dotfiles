
Terminator
===


### Installation

```bash
# Install `terminator` (arch linux)
pacman -S terminator

# Symlink `terminator`
ln -s ~/.dotfiles/terminator/terminator.symlink ~/.config/terminator/config

```
